# PROJECT MAP

> Auto-generated by `build_project_map.py` - do not edit by hand.
> Every GDScript file and function, with docstrings.

**14 files | 231 functions**

## res://scripts/element_grid/grid_reactions.gd

The chemical reaction engine -- the third system beside liquid and solid
movement. Soak trades pool water for damp; the fire solver burns fuel, boils
water and damp up the tiered ladder, vents smoke, and engulfs the tile; ignition counts contact ticks toward catch; condensation rains steam off cool stone away from fire; the tag pass refreshes wet flags; the packet assert closes the tick (reactions run last).

- `_init(w: int, h: int, terrain: GridStone) -> void` - Bind the packet via the stone facade and size the overlay state; reactions hold no matter of their own.
- `clear() -> void` - Reset the reaction state: ignition progress and the suppression mask. Packet columns clear through their owners.
- `tick() -> void` - One tick: soak, then the fire solver, then ignition and condensation, then tags (reactions run last, so the end-of-tick assert is theirs). Soak precedes fire by ruling: the puddle drinks before the flame boils what is left of it.
- `ignite(x: int, y: int) -> bool` - God-hand ignite at (x, y): fuel catches when its gates pass, bare air sparks one dying bit, wet fuel refuses. Returns true when something lit (the spark counts).
- `get_ignition(x: int, y: int) -> int` - Ignition progress at (x, y) -- contact ticks toward catch; the HUD's window into the timer.
- `_fire_pass() -> void` - The fire solver, every burning tile in index order: starve unfed flame, smother without air, burn constant, engulf one bit, boil each bit up the ladder, vent smoke by capacity. Bits pace everything but the burn -- burn is per tile and never reads ventilation. No projection: the flame above fuel is the renderer's lick, and a tile's fire lives exactly as long as its own fuel.
- `_burnable(i: int) -> bool` - May this tile's own flame burn fuel: the FUEL field, pool oil, or both. -- the spark's bit carries neither and dies at the starve.
- `_air_ok(i: int, x: int, y: int) -> bool` - The air check: the tile itself or any orthogonal holds pool_free >= AIR_MIN, smoke and steam counted -- fuel breathes through its neighbors (the all-adjacent doctrine).
- `_boil_one(i: int, x: int, y: int) -> void` - One fire bit walks the boil ladder: W0, D0, W1, D1, W2, D2 -- the first passing check only, capped by what is there, one source per bit. Standing water boils at twice the damp rate at every distance, in place where it stands; damp-steam is created matter and vents from its source tile. The distance rings ignore occlusion by ruling: stone-slowed distant boil is the readable distinction.
- `_emit(src: int, x: int, y: int, m: int, amount: int) -> void` - Vent created matter (smoke, damp-steam): the source tile's own headroom first, then the breathiest orthogonal, then the next — descending headroom, compass order breaking ties — so the chimney is a neighbor like any other, never the first-choice dump. Whatever fits is booked; the remainder is un-made -- no debt, no buffer, no rate change.
- `_ignition_pass() -> void` - Ignition delay: cold fuel in orthogonal contact accrues one progress tick per tick of joint contact and eligibility; any lapse resets it (the timer is the material's, so multiple burning neighbors never stack it). Catches land from a pending list after the sweep -- same-pass catches must not cascade contact, or the stagger drops to four ticks and the front walks 2.5 tiles a second.
- `_contact(x: int, y: int) -> bool` - True when any orthogonal neighbor carries fire bits -- the ignition stimulus; no diagonals, and a tile's own flame does not count.
- `_eligible(i: int, x: int, y: int) -> bool` - Catch gates: solid fuel refuses while any damp holds, oil refuses while water sits on top of it (the float layer catches, the submerged waits), and whatever catches needs the air check. Both gates apply when both fuels share a tile.
- `_condense_pass() -> void` - Condensation: steam rains back into water on cool stone, two units per tick in place, suppressed within Chebyshev FIRE_DIST of any active fire (the mask is rebuilt from the fire bits every tick). In place and net-zero, so it always fits.
- `_stone_touch(i: int) -> bool` - True when an orthogonal neighbor is STONE terrain -- the cool surface steam condenses against (wood insulates).
- `_soak_pass() -> void` - Soak pass: each water-bearing tile drinks clampi(w/16, 1, 16) per tick into the first headroom down its column -- its own soil, else through saturated soil below. Water never soaks across air, water-only tiles, or solid terrain.
- `_soak_target(i: int) -> int` - The walk: drink at the first headroom down the column from i, passing through saturated soil (the percolation skip). Stops at the bottom, solid terrain, or the first soil-less tile.
- `_tag_pass() -> void` - Refresh wet tags from damp: gain above 50% of capacity, lose below 30% -- hysteresis, so the tag is state that survives the band. Zero-capacity tiles never carry it.
- `snapshot_ignition() -> PackedByteArray` - The ignition overlay, value-copied for a room snapshot; the timer is persistent, the suppression mask is not.
- `restore_ignition(b: PackedByteArray) -> void` - Write the ignition overlay back and blank the suppression mask -- it rebuilds from the fire bits on the next tick.

## res://scripts/element_grid/grid_sand.gd

One tick: expand, run the sand pass, repack and book. Water's displacement pass resolves
repack's deficits same-tick; the packet assert is the reaction tick's (last engine's
privilege, per world §3).

- `_init(w: int, h: int, terrain: GridStone, p_water: GridWater) -> void` - Size the field, bind the packet via the stone facade and the water field for headroom queries.
- `idx(x: int, y: int) -> int` - Flat-array index of tile (x, y).
- `total(kind: int) -> int` - Total loose-solid subtiles of one kind (K_STONE/K_SOIL/K_ICE) in the field -- the mass checksum.
- `clear() -> void` - Reset: zero every subtile column and the tick counter.
- `tick() -> void` - One tick: expand, run the sand pass, repack and book. The paired water tick owns the packet assert -- its displacement pass resolves repack's deficits same-tick.
- `_expand() -> void` - Rebuild the expanded material grid from the packet nibbles (read-only).
- `_stamp_nibble(base: int, n: int, kind: int) -> void` - Write one nibble's set bits into the expanded grid as that kind's code.
- `_repack() -> void` - Gather the expanded grid back into per-kind nibbles; set_sub books every popcount delta.
- `_pass_all() -> void` - Run _sub_pass over every subtile cell, bottom-up, sweep alternating per tick.
- `_sub_pass(sx: int, sy: int) -> void` - Apply fall / slide to one subtile; d is the medium-dependent rate -- FALL[kind] through air, SUB_SINK[kind] through liquid -- taken as gated single-cell sub-steps so a fast sinker cannot tunnel. d <= 0 (buoyant kinds) rests: rising is the float lab, not this one.
- `_try_move(sx: int, sy: int, tx: int, ty: int, kind: int) -> bool` - Move a subtile: target cell empty, tile enterable, diagonals also need the side cell clear and the slide policy; a tile crossing exports flow and hands off the mover's share of damp.
- `_side_clear(sx: int, sy: int) -> bool` - Corner rule: the side cell must hold no subtile of any kind and sit in pool-capable terrain.
- `_cells_in_tile(x: int, y: int) -> int` - Count subtile cells currently in tile (x, y) per the live expanded grid -- pre-repack truth, so same-pass landings are not missed. Caller guarantees in-bounds.
- `_enterable(x: int, y: int) -> bool` - May a subtile enter tile (x, y): terrain must hold a pool, and the landing either fits the budget or the displaced liquid has escape-reachable headroom up the column (air-gate family, world §3). Caller guarantees in-bounds.
- `_can_slide(kind: int, i: int) -> bool` - May kind take a diagonal move? The mortar seam: a wet-tagged tile reads the sticky row -- slide-off (and lateral creep, when it exists) gates here, fall never does. Tags are one tick stale (the _escape pattern).
- `_in_liquid(sx: int, sy: int) -> bool` - Is this subtile wetted? Its own tile holds pool content. A full solid tile holds no pool; neighbor reads are the float lab's extension, not soil's.
- `flow_stamp(i: int, dir: int, mag: int) -> void` - Register a subtile arrival at tile i (one mass quantum, dir code); magnitudes sum, direction follows the largest single arrival (first stamp wins ties -- fixed sweep order, so deterministic).
- `get_flow_mag(x: int, y: int) -> int` - Flow strength at (x, y): subtile mass arrived this tick, 64 pool-units per subtile; 0 outside.
- `get_flow_dir(x: int, y: int) -> int` - Dominant flow direction at (x, y); NONE outside.
- `_cross_tile(sx: int, sy: int, tx: int, ty: int, kind: int) -> void` - Book a tile crossing: export the arrival's flow mass, then hand off damp -- soil only (stone and ice hold none), an even share of the source's damp over its remaining soil subtiles, capped at one subtile's worth. Live counts on both sides keep the transfer inside capacity by construction (the carry proof).
- `_soil_in_tile(x: int, y: int) -> int` - Count soil subtile cells in tile (x, y) per the live expanded grid -- damp's only carriers and only capacity. Caller guarantees in-bounds.

## res://scripts/element_grid/grid_stone.gd

Static terrain layer — a facade over the room's TilePacket.
The packet owns every column; this class owns the terrain RULES:
out-of-bounds reads as STONE, solidity, and the change signal.

- `_init(w: int, h: int) -> void` - Allocate the w×h grid as a fresh TilePacket; every tile air.
- `idx(x: int, y: int) -> int` - Flat-array index of tile (x, y).
- `in_bounds(x: int, y: int) -> bool` - True if (x, y) lies inside the grid.
- `get_terrain(x: int, y: int) -> int` - Terrain value at (x, y); out-of-bounds reads as STONE.
- `is_solid(x: int, y: int) -> bool` - True if the tile blocks flow (now checks for all solids).
- `set_terrain(x: int, y: int, t: int) -> bool` - Set terrain at (x, y); true (with emit) only when the value changed.
- `clear() -> void` - Reset every tile to AIR. Terrain only — the pool columns are not ours.

## res://scripts/element_grid/grid_water.gd

The liquid engine — a view over the shared TilePacket. Every pool liquid runs the four
movement rules as its own densest-first pass (viscosity-throttled), then the sort pass
trades densities; gases rise, spread laterally when blocked, and hop up-diagonal only around corners — exchanging ratios across horizontal faces — and seek level stays a liquid-only rule.

- `_init(w: int, h: int, terrain: GridStone) -> void` - Size the field, bind the packet via the stone facade; everything starts dry.
- `idx(x: int, y: int) -> int` - Flat-array index of tile (x, y).
- `in_bounds(x: int, y: int) -> bool` - True if (x, y) lies inside the grid.
- `get_water(x: int, y: int) -> int` - Water units at (x, y), 0..255; out-of-bounds reads as 0.
- `set_water(x: int, y: int, v: int) -> bool` - Overwrite the water at (x, y) toward v, clamped 0..255; refuses on full solids by capacity (pool_free = 0); false only when out of bounds.
- `add_water(x: int, y: int, amount: int) -> bool` - Add water units at (x, y); respects tile capacity; false if solid or OOB.
- `get_lines(x: int, y: int) -> int` - Visible scanlines at (x, y) — water units per LINE.
- `get_level(x: int, y: int) -> int` - Coarse broadcast band (Level) at (x, y); DRY outside the grid.
- `is_air_passable(x: int, y: int) -> bool` - True if the cell is not solid and holds at most a sub-visible film of any liquid.
- `get_region_of(x: int, y: int) -> int` - Air-pocket id at (x, y), or -1 when none or out of bounds.
- `is_air_sealed(x: int, y: int) -> bool` - Debug/renderer helper: true if this cell's air cannot reach open sky.
- `flow_stamp(i: int, dir: int, mag: int) -> void` - Register matter arriving at tile i (mag units, dir code); magnitudes sum, direction follows the largest single arrival (first stamp wins ties — sweep order is fixed, so deterministic); sim moves stamp inline, room sources like rain stamp at tick head.
- `get_flow_mag(x: int, y: int) -> int` - Flow strength at (x, y): total units that arrived this tick; 0 outside.
- `get_flow_dir(x: int, y: int) -> int` - Dominant flow direction at (x, y); NONE outside.
- `total() -> int` - Total water units in the field — the leak-check checksum.
- `clear() -> void` - Reset every pool column, the tick counter, and all bookkeeping to a dry, unlabelled state.
- `tick() -> void` - One simulation tick: relabel, eject displacement, run every mover's rules densest-first (liquids fall and seek level, gases rise), stratify densities, verify volume, verify the ledger, emit band changes.
- `_two_mats_present() -> bool` - True when at least two materials hold units anywhere -- the sort pass needs a pair to trade.
- `_band(lines: int) -> int` - Map a scanline count to its coarse Level band.
- `_levels_snapshot() -> PackedByteArray` - Per-cell Level bands from before the tick, for change detection.
- `_emit_level_changes(before: PackedByteArray) -> void` - Emit levels_changed listing every cell whose band changed this tick.
- `_analyze() -> void` - Rebuild this tick's labels: merged liquid bodies (any material — one pressure network), air pockets, escape flags.
- `_flood_body(start: int) -> void` - Flood-fill one connected liquid body (any material — pressure transmits through mixtures), stamping its id as we go.
- `_flood_region(start: int) -> void` - Label one air pocket; record its highest contact per body (a rotation's upper junction).
- `_note_contact(n: int, body_top: Dictionary) -> void` - Record a pocket cell's orthogonal neighbor as a body contact, keeping the highest row per body.
- `_compute_escape() -> void` - Mark every cell whose air can reach open sky; a monotone fixpoint, so order never matters.
- `_is_air_idx(i: int) -> bool` - Flat-index form of is_air_passable.
- `_xy_of(i: int) -> Vector2i` - Tile coordinates of flat index i.
- `_segment_top(x: int, y: int, m: int) -> int` - Topmost row of the contiguous visible-material-m segment containing (x, y). Caller guarantees m >= LINE at (x, y); with no visible segment the walk returns y + 1 -- a sentinel row, not a coordinate.
- `_cell_pass_all(m: int) -> void` - Run _cell_pass over every cell in this tick's sweep order.
- `_cell_pass(i: int, m: int) -> void` - Apply the fall / pour / creep rules to one cell for material m, in priority order; every move is capped by m's viscosity.
- `_gate(t: int, src: int, m: int) -> bool` - May material m from src enter dry cell t? Only if the displaced air has somewhere to go.
- `_seek_level_pass(m: int) -> void` - Rule four: try to equalize pressure across each run of liquid tiles (any material — mixtures are one conduit) for material m.
- `_seek_level_run(x0: int, x1: int, y: int, m: int) -> void` - Level one horizontal run of material m by pressure: heads are density-weighted column integrals from each surface to the run row -- true hydrostatic pressure at the choke. Donors must carry m at the conduit row; every column of the run may receive.
- `_transfer_level(s: int, t: int, y: int, diff: int, ptops: PackedInt32Array, m: int) -> int` - Move volume of material m from column s to receiver t so their pressures converge; returns units moved. Capped by half the pressure difference (no overshoot) and viscosity; the deposit lands at m's surface, the pool floor when m sinks, or the pool surface when m floats -- a full entry makes room by displacement: lighter residents yield in place, a pure-m entry thickens at the interface above, and air above an m-surface is the threshold-gated rise.
- `_pocket_vented_for(cell: int, s: int, s_top: int) -> bool` - Can the air above the receiver's surface give way to this transfer? s and s_top name the donor's own receding headspace.
- `headroom_above(x: int, y: int) -> bool` - Escape-gated column walk: does some tile above (x, y) have pool headroom with air that reaches open sky? The entry gate for deficit landings; _escape is the one-tick-stale opinion (_gate's family).
- `_displacement_pass() -> void` - Rule five: every over-budget tile ejects its excess up its column -- solids sink, liquid climbs. Lightest material first; leftover excess persists (the entry gate should have prevented it).
- `_checksum_all() -> int` - The all-material volume checksum -- read from the ledger (booked_pool_total); damp changes only in the reaction tick, which owns its own books. The ledger assert is the catch, not this one.
- `_sort_pass() -> void` - Density stratification: vertically adjacent tiles trade -- the densest material in the upper tile sinks, the lightest in the lower rises -- when the upper's is denser. Pair rate = the slower material's SORT_RATE. Top-down scan cascades the dense side; the light side rises into scanned rows -- one tile per tick.
- `_sort_pair(u: int) -> void` - Try one trade across the vertical pair at tile u (upper): densest-above vs lightest-below, swapped when denser-above. One trade per pair per tick -- the pacing knob. Take-both-then-add-both, so a full tile's freed budget always covers the incoming units.
- `get_total(x: int, y: int) -> int` - Total pool content (all materials) at (x, y); 0 outside the grid.
- `add_liquid(x: int, y: int, m: int, amount: int) -> bool` - Add liquid units of material m at (x, y); respects tile capacity; false if solid or OOB.
- `_pool_top(x: int, y: int) -> int` - Topmost row of the contiguous visible-liquid segment (any material) containing (x, y). Caller guarantees pool_total >= LINE at (x, y); with no segment the walk returns y + 1 -- a sentinel row, never a coordinate (the _segment_top contract).
- `_pool_bottom(x: int, y: int) -> int` - Bottom-most row of the contiguous visible-liquid segment (any material) containing (x, y). Caller guarantees pool_total >= LINE at (x, y); the walk is bounded by the grid.
- `_eject_lightest_up(i: int, amount: int) -> int` - Eject up to amount units of the lightest materials from tile i up its column, depositing at the first free escape-reachable tiles -- rule five's incompressibility walk, factored: the displacement pass and the lateral deposit share it. Returns units ejected; flow-stamped UP per deposit tile.
- `_is_gas(m: int) -> bool` - True for the rising materials: smoke and steam ride MOVERS but run their own rules — rise first, creep only when pinned, exchange ratios horizontally, never seek level (a gas column is not a pressure vessel; the sort pass is the only elevator a gas needs through liquid).
- `_gas_cell_pass_all(m: int) -> void` - Run _gas_cell_pass over every cell top-down, sweep alternating per tick: rise mirrors fall's bottom-up — scanning against the motion, so arrived gas is never reprocessed and a bubble climbs one tile per tick.
- `_gas_cell_pass(i: int, m: int) -> void` - Rise and spread for one gas cell — the three-step stack: what fits goes up (a transit column pistons whole and returns — the chimney ruling); the blocked remainder spreads LATERALLY at its own level, half-difference into lower-gas neighbors both sides; only a cell blocked above AND beside hops up-diagonal — corner rounding, never the primary dispersal, because order IS policy: hop-first grows a backed-up pile into a 45-degree cone, lateral-first flattens it into a mushroom. Rise and the hop are volume-neutral swaps with the air they enter (no air-gate); the hop is corner-gated so gas never leaks through a pinched diagonal.
- `_exchange_pass() -> void` - Horizontal gas exchange (rule six, diffusion-shaped): each gas trades half its difference between adjacent gas-holding tiles, capped by GAS_SWAP — the equalizer seek level refuses to be; runs after the movers and before the sort, so the tick ends stratified.
- `_exchange_pair(a: int, b: int) -> void` - Trade both gases between horizontal neighbors a and b: per gas half the difference (capped by GAS_SWAP), taking from both givers BEFORE any add — a full tile's inflow is covered by its own simultaneous outflow — and refused adds refund the giver, so scarce capacity pinches but never destroys.

## res://scripts/element_grid/room.gd

One CA domain (world.md §8): the engine quartet around one packet, the
tick ruling, the per-room PRNG, and the snapshot/restore machinery.
Unobserved rooms are simply never ticked -- freezing is absence of tick.

- `_init(p_w: int, p_h: int, p_seed: int) -> void` - Construct the full quartet around one packet (absent materials make idle engines nearly free) and seed the room PRNG -- advanced only inside ticks, never from the frame.
- `tick() -> void` - One simulation tick in the engine ruling: bridge slot reserved at the head (E4), solids, liquids, reactions last -- the ordering law's single owner.
- `snapshot() -> RoomState` - Copy the persistent census: fourteen packet columns, the ignition overlay, both sweep-parity counters.
- `restore(s: RoomState) -> void` - Write the census back, rebuild the maintained caches, end on the packet assert -- restore is sound outside the tick, not just before the next one.
- `reset() -> void` - Reset to a blank world through every owning clear path, terrain first (a cleared WOOD tile reads no damp cap); ends on the assert.

## res://scripts/element_grid/room_state.gd

A room's persistent census as value copies (world.md §8): fourteen packet
columns, the ignition overlay, both sweep-parity counters. Derived state
is never stored -- every engine rebuilds it at its tick head.

- `same_bytes(b: RoomState) -> bool` - Byte-exact census comparison -- the restore proofs' one call.

## res://scripts/element_grid/tile_packet.gd

The unified per-tile packet (world.md §2), stored structure-of-arrays:
one PackedByteArray column per field, one row per tile. The tile index
is the row number.

- `_init(p_w: int, p_h: int) -> void` - Allocate the w×h grid: every column resized, ledger zeroed.
- `clear() -> void` - Zero every column and the ledger.
- `idx(x: int, y: int) -> int` - Flat-array index of tile (x, y).
- `in_bounds(x: int, y: int) -> bool` - True if (x, y) lies inside the grid.
- `xy_of(i: int) -> Vector2i` - Tile (x, y) of flat index i.
- `get_terrain(i: int) -> int` - Terrain value at tile i.
- `set_terrain(i: int, t: int) -> bool` - Set terrain at tile i; true when the value changed (GridStone's signal rides on this).
- `get_sub(i: int, kind: int) -> int` - Subtile nibble at tile i; kind: 0 stone_s, 1 soil_s, 2 ice_s (matches SUB_SINK order).
- `set_sub(i: int, kind: int, n: int) -> int` - Set a nibble column, book the popcount delta; returns the capacity deficit created (ejecting that overflow is the flow engine's job, world §3).
- `pool_capacity(i: int) -> int` - Full-solid terrain = 0, otherwise 255 minus 64 per solid subtile (stone + soil + ice).
- `get_pool(i: int, m: int) -> int` - Units of material m held at tile i.
- `pool_total(i: int) -> int` - Units of every material held at tile i.
- `pool_free(i: int) -> int` - Headroom left at tile i (capacity minus total).
- `_add_units(i: int, m: int, amount: int) -> void` - The ONLY place pool bytes are mutated. Pre-clamped by callers.
- `add_pool(i: int, m: int, amount: int) -> int` - Add up to amount units of m; returns units accepted (refused units are NOT destroyed -- the caller ejects them by flow rules).
- `take_pool(i: int, m: int, amount: int) -> int` - Remove up to amount units; returns units actually taken.
- `set_pool(i: int, m: int, v: int) -> int` - Tool path (paint/erase): force tile i's m toward v; returns spill (units refused). Never destroys other materials or pushes past capacity (over-budget stays until flow ejects).
- `_book(row: int, delta: int) -> void` - Apply delta to the booked total of one ledger row.
- `mat_total(m: int) -> int` - Fresh recount of one pool column.
- `sub_total(kind: int) -> int` - Fresh popcount recount of one subtile column (kind: K_STONE/K_SOIL/K_ICE) -- the mass checksum.
- `assert_all() -> bool` - Double-entry check: booked totals vs fresh recounts, plus the per-tile pool constraint. Called at the end of every tick.
- `clear_damp() -> void` - Zero the damp and tag columns, booking the removal. (Clear/reset path.)
- `lightest_mat(i: int) -> int` - Material id of the single lowest-density content at tile i; -1 when the pool is empty.
- `drain_lightest(i: int, amount: int) -> int` - Remove up to amount units of the single lowest-density material present at tile i; returns units taken.
- `clear_mat(m: int) -> void` - Zero one pool column, booking the removal. (Clear/reset path.)
- `get_damp(i: int) -> int` - Damp held by the soil at tile i; outside the content pool (world §2), 32 capacity per soil subtile.
- `damp_capacity(i: int) -> int` - Damp capacity at tile i: 32 per soil subtile; a WOOD tile holds a flat 64 (half a full soil tile); everything else holds none.
- `add_damp(i: int, amount: int) -> int` - Add up to amount of damp, clamped by capacity; returns units accepted (callers pre-compute, refused units are theirs).
- `take_damp(i: int, amount: int) -> int` - Remove up to amount of damp; returns units actually taken.
- `shift_damp(src: int, dst: int, amount: int) -> void` - Transfer damp between tiles, no capacity clamp, no booking (net-zero by construction) -- the subtile carry path. The caller guarantees headroom by live subtile counts (GridSand's carry proof); the end-of-tick assert polices the invariant.
- `damp_total() -> int` - Fresh recount of the damp column -- the soak checksum at 1:1 water-equivalent.
- `has_tag(i: int, bit: int) -> bool` - True when the tag bit is set at tile i.
- `set_tag(i: int, bit: int, on: bool) -> void` - Set or clear one tag bit; tags are field state, never matter -- no booking.
- `pool_damp_total() -> int` - Sum of every pool material plus damp -- the harness conservation base (subtile matter rides its own ledger rows).
- `has_mat(m: int) -> bool` - True when any tile holds nonzero units of m -- the absent-material pass gate.
- `booked_pool_total() -> int` - Booked sum of the six pool rows -- O(1). Sound between asserts because a green assert proves booked == counted; a take-without-add leak still moves the booked total.
- `get_fuel(i: int) -> int` - Fuel held at tile i (0..255) -- burnable energy attached to solids, outside the content pool.
- `add_fuel(i: int, amount: int) -> int` - Add up to amount of fuel at tile i, clamped to the 255 ceiling; returns units accepted.
- `take_fuel(i: int, amount: int) -> int` - Remove up to amount of fuel at tile i; returns units actually taken.
- `set_fuel(i: int, v: int) -> void` - Tool path (paint/erase): force tile i's fuel toward v, booked; 255 is the only ceiling, so no spill exists.
- `fuel_total() -> int` - Fresh recount of the fuel column -- row eleven's checksum.
- `get_fire(i: int) -> int` - Fire subtile bits at tile i, same nibble layout as the solid kinds; overlay state -- coexists with terrain, solid subtiles, and pool alike.
- `set_fire(i: int, n: int) -> void` - Set the fire nibble at tile i -- god hands and the fire solver both route here, the single writer that keeps has_fire honest; no booking, fire is state not matter.
- `has_fire() -> bool` - True when any tile holds fire bits -- the fire solver's absent-pass gate, _present's pattern.
- `clear_fuel() -> void` - Zero the fuel column, booking the removal. (Clear/reset path.)
- `clear_fire() -> void` - Zero the fire nibbles and the present count. (Clear/reset path — overlay state, nothing booked.)
- `column(i: int) -> PackedByteArray` - Live reference to persistent column i, census order (0..COL_COUNT-1) -- duplicate before storing it anywhere: packed arrays are references.
- `load_column(i: int, b: PackedByteArray) -> void` - Point persistent column i at bytes (restore path); the write bypasses books, present counts, and fire_present -- the caller rebuilds them.
- `rebuild_books() -> void` - Recompute every booked row from fresh recounts -- the restore path's ledger rebuild; green asserts prove maintained == recounted, so this arrives consistent.
- `rebuild_present() -> void` - Recompute the present counts from the columns -- the absent-pass gates' bookkeeping, bypassed by wholesale restore writes; restore-time only, never per tick.

## res://scripts/fx/element_renderer.gd

world.md §9: one Image the size of the map, regenerated at tick rate, pushed
through an ImageTexture, drawn under actors. The sim state *is* the picture.
Regenerating 240×240 RGBA ten times a second is nothing — keep it dumb.

- `_init(terrain: GridStone, field: GridWater) -> void` - Bind the sim pair and create the map-sized image and texture.
- `redraw() -> void` - Repaint every tile plus overlays into the texture; call once per tick.
- `_draw_tile(x: int, y: int) -> void` - Paint one tile: beveled stone or planked wood (fire quads ride either), gases dithered from the top, liquids leveling from the bottom over them, soil quads over the fill they displace, fire bits on top of everything the tile holds.
- `_draw_debug() -> void` - Overlay air pockets, per-segment level lines, and the tile grid (G).
- `_draw_cursor() -> void` - Draw the hover cursor as a tile outline.
- `_draw_flow(x: int, y: int, px: int, py: int) -> bool` - Waterfall streaks: downward flow at mag >= FLOW_STREAK with dir DOWN draws hashed vertical streaks keyed on (tile, tick_count) — hash-based, never the sim PRNG (world §1). Returns true when streaks were drawn; the caller then draws no fill or crest -- falling water is streaks, not pooled lines.
- `_draw_soil(x: int, y: int, px: int, py: int, n: int) -> void` - Loose soil: one 8x8 quad per set nibble bit, light lip on subtiles with no soil directly above, damp darkening from the top of the occupied region (8 damp per pixel line -- the creeping front).
- `_lifted_lines(v: int, n: int) -> int` - Waterline lift over the tile's own soil subtiles: a squeezed pool reads higher on the fill. v = raw lines (units >> 4), n = soil nibble; returns the drawn line count, clamped to 15.
- `_draw_water(x: int, y: int, px: int, py: int, n: int) -> void` - Draw one tile's liquids (the gases drew first, top-down; liquids render over gas by ruling): waterfall streaks replace the fill; a tile covered by liquid above fills its full height stacked by share; the surface tile fills by the subtile-lifted line count, stacked densest from the bottom, crest on the topmost material.
- `bind_sand(p_sand: GridSand) -> void` - Bind the solid field for its flow export; scenes without one skip the arrows.
- `_majority(i: int) -> int` - Majority liquid at tile i -- the streak color's proxy (ties and empties read as water).
- `_draw_gas(x: int, y: int, px: int, py: int) -> void` - Gases draw top-down, eight units per line: checker dither from the tile's top to a full tile at 128, then one solid line per further eight (ceil) until the tile reads fully solid at 255 -- half liquid density, sub-eight films draw nothing. Amount and ratio are separate reads: the row count comes from the tile's TOTAL gas, and the rows split across the gases by share, lightest band on top (the liquid stack partition, mirrored).
- `_liquid_total(i: int) -> int` - Liquid units at tile i -- the pool minus the two gases; lines, crests, and the covered check all read this, never pool_total.
- `_draw_fire(x: int, y: int, px: int, py: int) -> void` - Fire bits draw as 8x8 ember quads on the fuel tile plus dancing licks -- one per bit, in the bottom subtile row of the non-solid tile above, hash-keyed on (tile, tick, bit) so every flame tongues on its own phase; never the sim PRNG (world §1). The lick is pure render: flame is picture, air is rules.

## res://scripts/global/palette.gd

Every color the project draws with, collected in one class of constants.
Day-one discipline (world.md §9): cold blue-gray base, water blues, and — since
the fire lab — the warm bank: wood tans, gases, the fire cycle. Swap for DB16 when real art starts.


## res://scripts/testing/test_fire.gd

Fire sandbox -- the fire lab whole: gases below, the fire solver, ignition
delay, and condensation, with the gas and fire acceptance suites on K.
1 stone / 2 water / 3 oil / 4 wood / 5 steam / 6 smoke / 7 soil / 8 damp, I ignites at hover, LMB doses, RMB erases.

- `_setup() -> void` - Build the full engine stack and load the rise demo.
- `_dose() -> void` - Tick-boundary dosing: held-mouse liquids and gases at PAINT_DOSE, soil one subtile, damp at DAMP_DOSE, wood whole tiles.
- `_clear_world() -> void` - Reset the sand engine, the damp books, the fire columns, and the reaction overlays along with the shared world.
- `_paint_stroke(t: Vector2i) -> bool` - Held strokes: RMB erases everything the fire lab can author; LMB lays stone over cleared cells.
- `_place_wood(t: Vector2i) -> void` - Lay a wood tile with a full fuel tank (god hand -- no flow stamp, no fire; refuses occupied ground).
- `_brush_soil(t: Vector2i) -> void` - Paint one soil subtile into the lowest empty slot of tile t (brush is a god hand -- no flow stamp).
- `_god_ignite() -> void` - God ignite at the hover tile: fuel catches if its gates pass; bare air sparks; wet fuel hisses.
- `_handle_key(k: int) -> bool` - Scene keys: F1-F5, F7, F9-F12 demos, 1-8 select material, I ignites at hover, K runs both suites.
- `_hint_header() -> String` - Controls listing for the fire sandbox.
- `_info_line(t: Vector2i) -> String` - Live readout: the base water line plus the hovered tile's fuel, fire, ignition timer, gases, and damp.
- `_load_preset(keycode: int) -> void` - Reset the full stack and carve the demo bound to a preset key.
- `drift_report(w0: PackedInt32Array, pk: TilePacket) -> String` - Fire-lab conservation policy: water, steam, and damp are one closed trio (boil and condensation trade inside it); fuel and oil burn to smoke at 4:1 as a production rate -- smoke never shrinks, and never grows faster than four per unit burned.
- `run_gas_tests() -> void` - Gas acceptance examples -- rise, ceiling spread, bubbling, the lip, the sealed cavity, stratification, and (revised for condensation) the rain cycle. Wood-lined where stone would drizzle.
- `run_fire_tests() -> void` - Fire acceptance examples -- the solver's core: front pace, burnout, the choke, the siege, the ladder, condensation distance, the climb. Timing tests run short and restore.

## res://scripts/testing/test_oil.gd

Oil sandbox — the liquid-variant lab: per-material passes, viscosity throttling,
and the density sort. 1 stone / 2 water / 3 oil select material, LMB paints,
RMB erases. K runs the oil acceptance suite headlessly and reports PASS/FAIL.

- `_setup() -> void` - Build the sand and reaction engines, load the flip demo.
- `_dose() -> void` - Tick-boundary dosing: held-mouse water or oil, dosed per material.
- `_clear_world() -> void` - Reset the sand engine along with the shared world.
- `_paint_stroke(t: Vector2i) -> bool` - Held strokes: RMB erases soil, liquids, and terrain; LMB lays stone over cleared cells.
- `_handle_key(k: int) -> bool` - Scene keys: F1-F4 presets, 1/2/3 material, K runs the oil suite.
- `_hint_header() -> String` - Controls listing for the oil sandbox.
- `_info_line(t: Vector2i) -> String` - Live readout: the base water line plus the hovered tile's oil, soil, and damp.
- `_load_preset(keycode: int) -> void` - Reset both engines and carve the demo bound to a preset key.
- `run_oil_tests() -> void` - Oil acceptance examples — sort, drain order, stratification, soil through oil.

## res://scripts/testing/test_sandbox.gd

Shared harness for element test scenes: owns the room (the engine quartet,
aliased as stone/water for the scenes' existing reads), renderer, tick
heartbeat, input plumbing, and paint/carve helpers. Scenes override hooks.

- `_ready() -> void` - Build the shared world, wire timer and signals, then hand off to _setup.
- `_setup() -> void` - Override: load the scene's initial state.
- `_on_tick() -> void` - Fixed-step heartbeat: dose held-mouse input, advance the sim, redraw.
- `_dose() -> void` - Override: apply held-mouse dosing that must land on tick boundaries.
- `_tick_world() -> void` - Override: advance the sim one tick -- the default runs the full engine ruling, owned by Room.tick (scenes no longer duplicate it).
- `_clear_world() -> void` - Override: reset beyond the blank world; the default is Room.reset (every column, book, overlay, counter).
- `_paint_stroke(_t: Vector2i) -> bool` - Override: handle held-mouse strokes; return true when the picture changed.
- `_process(_delta: float) -> void` - Track the hover tile; run held strokes; refresh the HUD.
- `_handle_key(_k: int) -> bool` - Override: extend key handling; return true when the key was consumed.
- `_unhandled_key_input(event: InputEvent) -> void` - Universal hotkeys (SPC pause, T step, X clear, G debug); the rest go to _handle_key.
- `_hover_tile() -> Vector2i` - Tile under the mouse, or (-1, -1) when off the grid.
- `_carve_preset(p_stone: GridStone, p_water: GridWater, rows: Array) -> Vector2i` - Carve a diagram (s stone, w water 255, o oil 255, d soil 15, t soil 12, W wood + fuel 255, v steam 255, m smoke 255, . air) wrapped in a stone U-shell; returns its origin.
- `_update_info(t: Vector2i) -> void` - Rebuild the HUD line (pause state, hover readout) when it changes.
- `_hint_header() -> String` - Override: the scene's controls listing shown above the live readout.
- `_info_line(t: Vector2i) -> String` - Override: the live readout for the hover tile.
- `_on_levels_changed(_cells) -> void` - No-op: redraws already ride the tick; keeps the signal wiring visible.
- `_run_example(name: String, rows: Array, check: Callable, setup: Callable) -> void` - Headless example runner, shared by every acceptance suite: carve rows into a fresh full engine stack, run optional god-hand setup before the snapshot, tick to equilibrium under a per-engine drift watch, then check + pool legality + policy-driven conservation + subtile mass. Drift reports on every failure, so expectation bugs and leaks never mask each other.
- `drift_report(w0: PackedInt32Array, pk: TilePacket) -> String` - Conservation policy for the acceptance runner: every material but water is individually constant, and water pairs with damp (soak's sanctioned 1:1 channel). Scenes whose reactions transform matter override with their own sanctioned channels.

## res://scripts/testing/test_soil.gd

Soil sandbox — solids, damp, and mortar over the shared packet.
1 stone / 2 water / 3 soil select material, LMB paints, RMB erases.
K runs the soil acceptance suite headlessly and reports PASS/FAIL.

- `_setup() -> void` - Build the sand and reaction engines, bind the renderer's solid flow, load the spread demo.
- `_dose() -> void` - Tick-boundary dosing: held-mouse water, or soil one subtile per tick.
- `_clear_world() -> void` - Reset the sand engine along with the shared world.
- `_paint_stroke(t: Vector2i) -> bool` - Held strokes: RMB erases soil, water, and terrain; LMB lays stone over cleared cells.
- `_handle_key(k: int) -> bool` - Scene keys: F1-F7 presets, 1/2/3 material, K runs the soil suite.
- `_hint_header() -> String` - Controls listing for the soil sandbox.
- `_info_line(t: Vector2i) -> String` - Live readout: the base water line plus the hovered tile's soil nibble, damp level, and wet tag.
- `_load_preset(keycode: int) -> void` - Reset both engines and carve the demo bound to a preset key.
- `_brush_soil(t: Vector2i) -> void` - Paint one soil subtile into the lowest empty slot of tile t (brush is a god hand -- no flow stamp).
- `_test(name: String, preset_key: int, check: Callable) -> void` - Carve one preset-key example and route it through the shared runner.
- `run_soil_tests() -> void` - Soil acceptance examples -- repose, corner rule, compaction.

## res://scripts/testing/water_test.gd

Water sandbox — element sim v2.
1 stone / 2 water select material, LMB paints, RMB erases.
K runs the six acceptance examples headlessly and reports PASS/FAIL.

- `_setup() -> void` - Load the U-tube demo on boot.
- `_dose() -> void` - Tick-boundary dosing: held-mouse water in PAINT_DOSE units.
- `_paint_stroke(t: Vector2i) -> bool` - Held strokes: RMB erases water and terrain; LMB lays stone over cleared water.
- `_handle_key(k: int) -> bool` - Scene keys: F1-F7 presets, 1/2 material, K runs the water and packet suites.
- `_hint_header() -> String` - Controls listing for the water sandbox.
- `_load_preset(keycode: int) -> void` - Reset both grids and carve the demo bound to a preset key.
- `run_self_tests() -> void` - Run the six acceptance examples headlessly and report PASS/FAIL.
- `run_packet_tests() -> void` - Packet unit tests — pure TilePacket behavior, no carving, instant.
- `_ptest(name: String, fn: Callable) -> void` - PASS/FAIL runner for the packet tests (mirror of _test, no carving).
- `_test(name: String, preset_key: int, check: Callable) -> void` - Carve one example fresh, run it to equilibrium, report PASS/FAIL/leak.
